import { ApplicationCongratulation } from "alapa";

export const appConfig: ApplicationCongratulation = {
  appUrl: "https://www.exampleapp.com",
  appName: "ExampleApp",
  debugMode: true, // Enable debug mode for development
  timezone: "America/New_York", // Default timezone
  locale: "en-US", // Default locale/language
  fallbackLocale: "en", // Fallback locale if the default locale is unavailable
  maintenanceMode: false, // Disable maintenance mode
  allowCors: true, // Enable CORS globally
  description: "This is a odu Project",
};
